This plugin supplies the OpenCV 4.8.0 native runtime used by AutoJs6 image APIs. AutoJs6 discovers it automatically after installation and enablement.

The plugin has no standalone user interface. Install it only from a trusted source. If the plugin is upgraded after AutoJs6 has loaded OpenCV, restart AutoJs6 before continuing image processing.
